## 📝 작업 내용

### ✨ 기능 구현
*   **마이페이지 V1 구현:**
    *   마이페이지의 전반적인 UI와 기능을 구현했습니다.
    *   `Avatar`, `ProfileCard`, `BadgeRow` 등 마이페이지 관련 컴포넌트를 개발했습니다.
*   **프로필 관련 기능 구현:**
    *   프로필 변경 화면 UI를 구현하고, 프로필 조회 API를 연결했습니다.
    *   프로필 이미지 업로드 기능을 추가했습니다. `react-native-image-picker`를 통해 이미지를 선택하고, Presigned URL을 발급받아 S3에 업로드 후 `profileImage` 상태를 업데이트합니다.
*   **설정 관련 기능 구현:**
    *   설정 화면 UI를 구현하고, 공통 모달 UI 및 로그인 API를 연결했습니다.
*   **API 연결 및 관리:**
    *   마이페이지 관련 API 및 챌린지 API를 연결하고, UI 업데이트 및 에러 메시지 처리를 개선했습니다.
*   **컴포넌트 개선:**
    *   `Button` 컴포넌트에 글자 크기 prop 및 outline 스타일을 추가했습니다.

### 🐛 버그 수정 및 UI/UX 개선
*   **SubpageHeader 통일:** `SubpageHeader` 컴포넌트를 `Header`로 통일하고 관련 파일을 업데이트했습니다.
*   **버튼 스케일 문제 해결:** `Button` 컴포넌트의 스케일로 인한 크기 변경 문제를 수정했습니다.
*   **'완료' 버튼 색상 로직 수정:** 프로필 편집 화면에서 변경 사항 유무에 따라 '완료' 버튼의 텍스트 색상이 다르게 표시되도록 로직을 개선했습니다.

### 🛠️ 리팩토링 및 기타
*   **Storybook 적용:** 다양한 컴포넌트에 Storybook을 적용하고 관련 설정을 업데이트했습니다.
*   **API 클라이언트 디버깅 로깅 추가 (src/libs/api/client.ts):** `401 Unauthorized` 에러 발생 시 토큰 재발급 과정을 상세하게 추적할 수 있도록 `axios` 응답 인터셉터에 `console.log`를 추가했습니다.

## 🔗 관련 이슈
<!-- 이 브랜치에서 해결하거나 참조하는 이슈가 있다면 여기에 추가해 주세요. -->

## ⚙️ PR 유형
- [x] 새로운 기능 (마이페이지, 프로필, 설정 기능 구현 및 이미지 업로드)
- [x] 버그 수정 (SubpageHeader 통일, 버튼 스케일, '완료' 버튼 색상)
- [x] UI/UX 개선 (다양한 컴포넌트 UI 개선, 이미지 선택 UI)
- [x] 리팩토링 (Storybook 적용, API 클라이언트 로깅, 기존 TODO 구현)
- [ ] 의존성 변경

## 📸 스크린샷
<!-- 실제 PR에서는 해당 기능별 스크린샷을 첨부해 주세요. -->

## ⚠️ Notes
*   **`401 Unauthorized` API 문제 (프로필 업데이트 시):**
    로그 분석 결과, 클라이언트 측의 토큰 재발급 로직은 정상적으로 동작합니다. 하지만 백엔드 (`/api/v1/auth/reissue`)에서 새로 발급된 `accessToken`을 (`isSuccess: true`로 응답함에도 불구하고) 이후의 요청에서 `유효하지 않은 토큰입니다.`(`AUTH001`)라며 거부하고 있습니다. 이 문제는 **백엔드의 토큰 발급 및 유효성 검사 로직에 대한 확인이 필요**하며, 클라이언트 측에서 해결할 수 없습니다. 관련 백엔드 팀과 논의가 필요합니다.
*   **`TypeError: Cannot call a class as a function` 오류:**
    `Text` 컴포넌트 렌더링 과정에서 발생하는 이 오류는 주로 React Native 환경에서 캐시 손상이나 잘못된 번들링으로 인해 발생합니다. 이 문제를 해결하기 위해 **PR 머지 후 클린 빌드(Metro 캐시 초기화, 네이티브 빌드 폴더 삭제, `node_modules` 재설치 등)를 반드시 수행**해야 합니다.
*   **`userId` 확인:** 프로필 이미지 업로드 시 S3 파일명 생성에 `userId`를 사용하고 있습니다. `originalUser?.userId`가 `null`일 경우 'unknown'으로 기본 설정되도록 되어있으니, 실제 사용자 ID가 제대로 전달되는지 확인이 필요합니다.